# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_apiKey: ${{ secrets.NEXT_PUBLIC_apiKey }}
      NEXT_PUBLIC_authDomain: ${{ secrets.NEXT_PUBLIC_authDomain }}
      NEXT_PUBLIC_projectId: ${{ secrets.NEXT_PUBLIC_projectId }}
      NEXT_PUBLIC_storageBucket: ${{ secrets.NEXT_PUBLIC_storageBucket }}
      NEXT_PUBLIC_messagingSenderId: ${{ secrets.NEXT_PUBLIC_messagingSenderId }}
      NEXT_PUBLIC_appId: ${{ secrets.NEXT_PUBLIC_appId }}
      NEXT_PUBLIC_collection: ${{ secrets.NEXT_PUBLIC_collection }}
      NEXT_PUBLIC_collection_alerts: ${{ secrets.NEXT_PUBLIC_collection_alerts }}
      NEXT_PUBLIC_collection_summaire: ${{ secrets.NEXT_PUBLIC_collection_summaire }}
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SEIS_4D3F6 }}
          channelId: live
          projectId: seis-4d3f6
